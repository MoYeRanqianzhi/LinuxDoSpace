# Admin frontend environment example.
# This project is a static Vite application, so every VITE_* value is visible to the browser.
# Do not place any server-side secrets here.

VITE_API_BASE_URL=http://localhost:8080
